spring:
  datasource:
     url: jdbc:postgresql://localhost:5432/market
     username: postgres
     password: 123
  jpa:
     hibernate:
       ddl-auto: update
     show-sql: true
  output:
     ansi:
       enabled: always
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8801/eureka
      
logging:
  level:
    org:
      springframework:
        security: debug
debug: true
